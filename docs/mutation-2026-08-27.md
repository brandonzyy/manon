# manon 变异测试结论 · 2026-08-27

工具：`~/.claude/bin/quality/mutation_probe.py`（判据只此一份；刻意不留依赖）。
对象：`core/contract_audit/`——2026-08-27 当天判据翻转事故的主角，四根因里
contract 棘轮的审计面就住在这三个文件里。门禁：`tests/test_contract_audit.py`
（69 条，绿/红成对写法）+ 独立树（`git archive HEAD` 解出，probe 原地变异，
共用工作树不碰）。

| 文件 | 变异点 | 杀掉 | 存活 |
|---|---|---|---|
| endpoints.py | 261 | 123 | 138 |
| configs.py | 109 | 57 | 52 |
| files.py | 209 | 48 | 161 |
| 合计 | 579 | 228（39%） | 351 |

## 存活判读（逐类，不看总数）

1. **枚举表/字面量表的单条抽走——约七成**。`_HARD_SKIP_DIRS` 逐目录、
   `_KIND_BY_EXT` 逐扩展名、客户端绑定正则逐框架名（axios/ky/got…）、
   TS 扩展名元组逐条、`{2,}` 量词数字：单条变空只影响**本门禁夹具没铺到的
   语言面**。判据主链（route 匹配、dead/suspect 判定）不经过这些条目时，
   绿是正确行为。处置：属于覆盖循环的输入（夹具宽度），不是判据缺陷。
2. **tier 边界未到达——约两成**。configs.py:45 `return→None`（deploy 档
   整档无夹具）、:54 `".env." in name → not in`（中缀 .env 文件名无夹具）、
   endpoints.py:423 `kind != "sql"` 反转（sql 档边缘）。同上，覆盖缺口。
3. **等价变异**——日志/汇总字符串（endpoints.py:427 等），改了不改判定。

**「判据说谎」类存活：零。** 没有发现 verdict 主判定、ratchet 比较逻辑被改坏
而门禁仍绿的案例；当天的翻转事故源于审计面边界（已另修），不是判定逻辑。

## 真发现（本轮唯一）

**`files.py` 的 git 视角枚举分支（`_git_scope`）在首轮门禁夹具上不可达**：
probe 的临时树不是 git 仓，`enumerate_files` 走的全是回退走查分支——
2026-08-27 新加的「跟踪 + 未跟踪未忽略」边界逻辑没有被测到。
处置（当天已补）：test_contract_audit 新增 git 仓夹具（tracked / 未跟踪
未忽略 / gitignored 三类各一，断言只有前两类进 SourceFile）+ 非 git 树
回退走查一条。补后重跑 files.py：**杀掉 48 → 63，`_git_scope` 分支进入
守卫范围**；残余存活在 `_readable` 的防御路径（OSError / 软链 / 2MB 上限，
夹具宽度问题，列为覆盖循环输入）。

## 后续

- 覆盖循环的输入：deploy 档、sql 档、中缀 .env、非 py/md 扩展名夹具。
- 一年后或 contract_audit 主判定逻辑再动时重跑（一次性动作，365 天过期）。
