# 契约对账豁免账 —— 判据实现：scripts/manon-contract-audit.py（零模型零服务端）。
# 每条死面只有两个去处：**退役**（删掉，这里不写）或**豁免**（写进下面并给 reason）。
# 「先放着」不存在——放着就是下轮重报，重报几次这张表就没人看了。
# 当前已知死面（13 条）不走豁免、走 scripts/l1-baselines/contract.txt 棘轮：
# 它们是待处置的存量，处置动作是退役或接线，不是豁免。
# reason 要能被下一个人核对：写清楚谁在用、为什么活着。

exempt: {}
